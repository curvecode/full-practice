import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
    },
    rowFlex: {
        flexDirection: 'row',
        flexWrap: 'wrap',
    }
});

export default styles;
